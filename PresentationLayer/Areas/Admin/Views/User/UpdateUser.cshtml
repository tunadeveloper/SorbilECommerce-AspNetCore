
@{
    ViewData["Title"] = "UpdateUser";
    Layout = "~/Areas/Admin/Views/_adminLayout.cshtml";
}
@model EntityLayer.Concrete.User


<div class="container-fluid">
    <h5 class="card-title fw-semibold mb-4">Kullanıcı Güncelleme Sayfası</h5>
    <div class="row">
        <div class="col-lg-12 d-flex align-items-stretch">
            <div class="card w-100">
                <div class="card-body p-4">
                    <form asp-action="UpdateUser" asp-area="Admin" asp-controller="User" method="post">
                        <label class="form-label">Kullanıcı Adı</label>
                        <input type="text" name="Username" class="form-control" value="@Model.Username">
                        <br />
                        <label class="form-label">Şifre</label>
                        <input type="password" name="Password" class="form-control" value="@Model.Password">
                        <br />
                        <button class="btn btn-info">Kullanıcıyı Güncelle</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>



